# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json

package:
  name: ros2-async-web-server-cpp
  version: 2.0.1
source:
  git: https://github.com/ros2-gbp/async_web_server_cpp-release.git
  tag: release/jazzy/async_web_server_cpp/2.0.1-1
  target_directory: ros2-async-web-server-cpp/src/work
  patches:
    - patch/ros-jazzy-async-web-server-cpp.patch

build:
  script: ${{ '$RECIPE_DIR/build_catkin.sh' if unix or wasm32 else '%RECIPE_DIR%\\bld_catkin.bat' }}
  number: 21
  post_process:
    - files:
        - '*.pc'
      regex: (?:-L|-I)?"?([^;\s]+/sysroot/)
      replacement: $$(CONDA_BUILD_SYSROOT_S)
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/sysroot)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/MacOSX\d*\.?\d*\.sdk)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
about:
  homepage: http://ros.org/wiki/async_web_server_cpp
  repository: https://github.com/fkie/async_web_server_cpp
  license: BSD-3-Clause
  summary: Asynchronous Web/WebSocket Server in C++
extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - ${{ compiler('cxx') }}
    - ${{ compiler('c') }}
    - if: target_platform!='emscripten-wasm32'
      then:
        - ${{ stdlib('c') }}
    - ninja
    - python
    - setuptools
    - git
    - git-lfs
    - if: unix
      then:
        - patch
        - make
        - coreutils
    - if: win
      then:
        - m2-patch
    - if: osx
      then:
        - tapi
    - if: build_platform != target_platform
      then:
        - pkg-config
    - cmake
    - cython
    - if: build_platform != target_platform
      then:
        - python
        - cross-python_${{ target_platform }}
        - numpy
    - if: osx
      then:
        - clang-tools ${{ (cxx_compiler_version ~ '.*') if cxx_compiler_version is defined else '*' }}
  host:
    - if: build_platform == target_platform
      then:
        - pkg-config
    - libboost-devel
    - libboost-python-devel
    - numpy
    - openssl
    - pip
    - python
    - ros2-ament-cmake-ros
    - ros2-distro-mutex 0.16.* jazzy_*
    - ros2-launch-testing
    - ros2-ros-environment
    - ros2-ros-workspace
    - websocket-client
  run:
    - libboost-devel
    - libboost-python-devel
    - python
    - ros2-distro-mutex 0.16.* jazzy_*
    - ros2-ros-workspace
