# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json

package:
  name: ros2-web-video-server
  version: 3.1.0
source:
  git: https://github.com/ros2-gbp/web_video_server-release.git
  tag: release/jazzy/web_video_server/3.1.0-1
  target_directory: ros2-web-video-server/src/work
  patches:
    - patch/ros-jazzy-web-video-server.patch

build:
  script: ${{ '$RECIPE_DIR/build_ament_cmake.sh' if unix or wasm32 else '%RECIPE_DIR%\\bld_ament_cmake.bat' }}
  number: 21
  post_process:
    - files:
        - '*.pc'
      regex: (?:-L|-I)?"?([^;\s]+/sysroot/)
      replacement: $$(CONDA_BUILD_SYSROOT_S)
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/sysroot)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/MacOSX\d*\.?\d*\.sdk)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
about:
  homepage: http://ros.org/wiki/web_video_server
  repository: https://github.com/RobotWebTools/web_video_server
  license: BSD-3-Clause
  summary: HTTP Streaming of ROS Image Topics in Multiple Formats
extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - ${{ compiler('cxx') }}
    - ${{ compiler('c') }}
    - if: target_platform!='emscripten-wasm32'
      then:
        - ${{ stdlib('c') }}
    - ninja
    - python
    - setuptools
    - git
    - git-lfs
    - if: unix
      then:
        - patch
        - make
        - coreutils
    - if: win
      then:
        - m2-patch
    - if: osx
      then:
        - tapi
    - if: build_platform != target_platform
      then:
        - pkg-config
    - cmake
    - cython
    - if: build_platform != target_platform
      then:
        - python
        - cross-python_${{ target_platform }}
        - numpy
    - if: osx
      then:
        - clang-tools ${{ (cxx_compiler_version ~ '.*') if cxx_compiler_version is defined else '*' }}
  host:
    - if: build_platform == target_platform
      then:
        - pkg-config
    - ffmpeg
    - libboost-devel
    - libboost-python-devel
    - libgl-devel
    - libopencv
    - libopengl-devel
    - numpy
    - pip
    - pkg-config
    - py-opencv
    - python
    - ros2-ament-cmake-clang-tidy
    - ros2-ament-cmake-copyright
    - ros2-ament-cmake-lint-cmake
    - ros2-ament-cmake-ros
    - ros2-ament-cmake-uncrustify
    - ros2-ament-cmake-xmllint
    - ros2-ament-lint-auto
    - ros2-async-web-server-cpp
    - ros2-cv-bridge
    - ros2-distro-mutex 0.16.* jazzy_*
    - ros2-image-transport
    - ros2-pluginlib
    - ros2-rclcpp
    - ros2-rclcpp-components
    - ros2-rmw
    - ros2-ros-environment
    - ros2-ros-workspace
    - ros2-sensor-msgs
  run:
    - ffmpeg
    - libboost-devel
    - libboost-python-devel
    - libgl-devel
    - libopencv
    - libopengl-devel
    - py-opencv
    - python
    - ros2-async-web-server-cpp
    - ros2-cv-bridge
    - ros2-distro-mutex 0.16.* jazzy_*
    - ros2-image-transport
    - ros2-pluginlib
    - ros2-rclcpp
    - ros2-rclcpp-components
    - ros2-rmw
    - ros2-ros-workspace
    - ros2-sensor-msgs
