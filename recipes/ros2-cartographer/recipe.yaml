# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json

package:
  name: ros2-cartographer
  version: 2.0.0

build:
  number: 21
  script: ''
  post_process:
    - files:
        - '*.pc'
      regex: (?:-L|-I)?"?([^;\s]+/sysroot/)
      replacement: $$(CONDA_BUILD_SYSROOT_S)
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/sysroot)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/MacOSX\d*\.?\d*\.sdk)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
about:
  homepage: https://github.com/cartographer-project/cartographer
  license: Apache-2.0
  summary: "Cartographer is a system that provides real-time simultaneous localization\n    and mapping (SLAM) in 2D and 3D across multiple platforms and sensor\n    configurations."
extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - if: build_platform != target_platform
      then:
        - git
  host:
    - cairo
    - ceres-solver
    - eigen
    - eigen-abi-devel
    - gflags
    - glog
    - gmock
    - gtest
    - libabseil
    - libboost-devel
    - libprotobuf
    - lua
    - protobuf
    - python
    - ros2-distro-mutex 0.16.* jazzy_*
    - ros2-ros-environment
    - ros2-ros-workspace
    - sphinx
    - if: build_platform == target_platform
      then:
        - git
  run:
    - cairo
    - cartographer >=2.0.0, <2.1.0a0
    - ceres-solver
    - eigen
    - eigen-abi-devel
    - gflags
    - glog
    - libabseil
    - libboost-devel
    - libprotobuf
    - lua
    - protobuf
    - python
    - ros2-distro-mutex 0.16.* jazzy_*
    - ros2-ros-workspace
