# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json

package:
  name: ros2-rcl-yaml-param-parser
  version: 9.2.11
source:
  git: https://github.com/ros2-gbp/rcl-release.git
  tag: release/jazzy/rcl_yaml_param_parser/9.2.11-1
  target_directory: ros2-rcl-yaml-param-parser/src/work

build:
  script: ${{ '$RECIPE_DIR/build_ament_cmake.sh' if unix or wasm32 else '%RECIPE_DIR%\\bld_ament_cmake.bat' }}
  number: 21
  post_process:
    - files:
        - '*.pc'
      regex: (?:-L|-I)?"?([^;\s]+/sysroot/)
      replacement: $$(CONDA_BUILD_SYSROOT_S)
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/sysroot)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/MacOSX\d*\.?\d*\.sdk)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
about:
  license: Apache-2.0
  summary: Parse a YAML parameter file and populate the C data structure.
extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - ${{ compiler('cxx') }}
    - ${{ compiler('c') }}
    - if: target_platform!='emscripten-wasm32'
      then:
        - ${{ stdlib('c') }}
    - ninja
    - python
    - setuptools
    - git
    - git-lfs
    - if: unix
      then:
        - patch
        - make
        - coreutils
    - if: win
      then:
        - m2-patch
    - if: osx
      then:
        - tapi
    - if: build_platform != target_platform
      then:
        - pkg-config
    - cmake
    - cython
    - if: build_platform != target_platform
      then:
        - python
        - cross-python_${{ target_platform }}
        - numpy
    - if: osx
      then:
        - clang-tools ${{ (cxx_compiler_version ~ '.*') if cxx_compiler_version is defined else '*' }}
    - if: target_platform != 'emscripten-wasm32'
      then:
        - ros2-mimick-vendor
  host:
    - if: build_platform == target_platform
      then:
        - pkg-config
    - numpy
    - pip
    - python
    - ros2-ament-cmake-gen-version-h
    - ros2-ament-cmake-gtest
    - ros2-ament-cmake-ros
    - ros2-ament-lint-auto
    - ros2-ament-lint-common
    - ros2-distro-mutex 0.16.* jazzy_*
    - ros2-libyaml-vendor
    - ros2-osrf-testing-tools-cpp
    - ros2-performance-test-fixture
    - ros2-rcutils
    - ros2-rmw
    - ros2-ros-environment
    - ros2-ros-workspace
    - yaml
    - yaml-cpp
  run:
    - python
    - ros2-distro-mutex 0.16.* jazzy_*
    - ros2-libyaml-vendor
    - ros2-rcutils
    - ros2-rmw
    - ros2-ros-workspace
    - yaml
    - yaml-cpp
