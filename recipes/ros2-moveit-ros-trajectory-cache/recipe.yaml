# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json

package:
  name: ros2-moveit-ros-trajectory-cache
  version: 2.12.4
source:
  git: https://github.com/ros2-gbp/moveit2-release.git
  tag: release/jazzy/moveit_ros_trajectory_cache/2.12.4-1
  target_directory: ros2-moveit-ros-trajectory-cache/src/work

build:
  script: ${{ '$RECIPE_DIR/build_ament_cmake.sh' if unix or wasm32 else '%RECIPE_DIR%\\bld_ament_cmake.bat' }}
  number: 21
  post_process:
    - files:
        - '*.pc'
      regex: (?:-L|-I)?"?([^;\s]+/sysroot/)
      replacement: $$(CONDA_BUILD_SYSROOT_S)
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/sysroot)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/MacOSX\d*\.?\d*\.sdk)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
about:
  license: Apache-2.0
  summary: A trajectory cache for MoveIt 2 motion plans and cartesian plans.
extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - ${{ compiler('cxx') }}
    - ${{ compiler('c') }}
    - if: target_platform!='emscripten-wasm32'
      then:
        - ${{ stdlib('c') }}
    - ninja
    - python
    - setuptools
    - git
    - git-lfs
    - if: unix
      then:
        - patch
        - make
        - coreutils
    - if: win
      then:
        - m2-patch
    - if: osx
      then:
        - tapi
    - if: build_platform != target_platform
      then:
        - pkg-config
    - cmake
    - cython
    - if: build_platform != target_platform
      then:
        - python
        - cross-python_${{ target_platform }}
        - numpy
    - if: osx
      then:
        - clang-tools ${{ (cxx_compiler_version ~ '.*') if cxx_compiler_version is defined else '*' }}
  host:
    - if: build_platform == target_platform
      then:
        - pkg-config
    - numpy
    - pip
    - pytest
    - python
    - ros2-ament-cmake
    - ros2-ament-cmake-pytest
    - ros2-ament-cmake-uncrustify
    - ros2-distro-mutex 0.16.* jazzy_*
    - ros2-geometry-msgs
    - ros2-launch-pytest
    - ros2-launch-testing-ament-cmake
    - ros2-moveit-common
    - ros2-moveit-configs-utils
    - ros2-moveit-planners-ompl
    - ros2-moveit-resources
    - ros2-moveit-ros-planning-interface
    - ros2-rclcpp
    - ros2-rclcpp-action
    - ros2-rmf-utils
    - ros2-robot-state-publisher
    - ros2-ros-environment
    - ros2-ros-workspace
    - ros2-ros2-control
    - ros2-tf2-ros
    - ros2-trajectory-msgs
    - ros2-warehouse-ros-sqlite
  run:
    - python
    - pyyaml
    - ros2-distro-mutex 0.16.* jazzy_*
    - ros2-geometry-msgs
    - ros2-moveit-common
    - ros2-moveit-ros
    - ros2-moveit-ros-planning-interface
    - ros2-rclcpp
    - ros2-rclcpp-action
    - ros2-ros-workspace
    - ros2-tf2-ros
    - ros2-trajectory-msgs
    - ros2-xacro
