# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json

package:
  name: ros2-control-toolbox
  version: 4.11.1
source:
  git: https://github.com/ros2-gbp/control_toolbox-release.git
  tag: release/jazzy/control_toolbox/4.11.1-1
  target_directory: ros2-control-toolbox/src/work

build:
  script: ${{ '$RECIPE_DIR/build_ament_cmake.sh' if unix or wasm32 else '%RECIPE_DIR%\\bld_ament_cmake.bat' }}
  number: 21
  post_process:
    - files:
        - '*.pc'
      regex: (?:-L|-I)?"?([^;\s]+/sysroot/)
      replacement: $$(CONDA_BUILD_SYSROOT_S)
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/sysroot)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/MacOSX\d*\.?\d*\.sdk)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
about:
  homepage: https://control.ros.org
  repository: https://github.com/ros-controls/control_toolbox/
  license: Apache-2.0
  summary: The control toolbox contains modules that are useful across all controllers.
extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - ${{ compiler('cxx') }}
    - ${{ compiler('c') }}
    - if: target_platform!='emscripten-wasm32'
      then:
        - ${{ stdlib('c') }}
    - ninja
    - python
    - setuptools
    - git
    - git-lfs
    - if: unix
      then:
        - patch
        - make
        - coreutils
    - if: win
      then:
        - m2-patch
    - if: osx
      then:
        - tapi
    - if: build_platform != target_platform
      then:
        - pkg-config
    - cmake
    - cython
    - if: build_platform != target_platform
      then:
        - python
        - cross-python_${{ target_platform }}
        - numpy
    - if: osx
      then:
        - clang-tools ${{ (cxx_compiler_version ~ '.*') if cxx_compiler_version is defined else '*' }}
  host:
    - if: build_platform == target_platform
      then:
        - pkg-config
    - cpp-expected
    - eigen
    - eigen-abi-devel
    - fmt
    - numpy
    - pip
    - python
    - ros2-ament-cmake
    - ros2-ament-cmake-gmock
    - ros2-backward-ros
    - ros2-control-msgs
    - ros2-distro-mutex 0.16.* jazzy_*
    - ros2-filters
    - ros2-generate-parameter-library
    - ros2-geometry-msgs
    - ros2-pluginlib
    - ros2-rclcpp
    - ros2-rclcpp-lifecycle
    - ros2-rcutils
    - ros2-realtime-tools
    - ros2-ros-environment
    - ros2-ros-workspace
    - ros2-ros2-control-cmake
    - ros2-rsl
    - ros2-tf2
    - ros2-tf2-geometry-msgs
    - ros2-tf2-ros
  run:
    - cpp-expected
    - eigen
    - eigen-abi-devel
    - fmt
    - python
    - ros2-backward-ros
    - ros2-control-msgs
    - ros2-distro-mutex 0.16.* jazzy_*
    - ros2-filters
    - ros2-generate-parameter-library
    - ros2-geometry-msgs
    - ros2-pluginlib
    - ros2-rclcpp
    - ros2-rcutils
    - ros2-realtime-tools
    - ros2-ros-workspace
    - ros2-rsl
    - ros2-tf2
    - ros2-tf2-geometry-msgs
    - ros2-tf2-ros
tests:
  - script:
      - cmake-package-check control_toolbox --targets control_toolbox::control_toolbox control_toolbox::rate_limiter control_toolbox::low_pass_filter
    requirements:
      run:
        - cmake-package-check
        - ${{ compiler('c') }}
        - ${{ compiler('cxx') }}
