# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json

package:
  name: ros2-grid-map-demos
  version: 2.2.2
source:
  git: https://github.com/ros2-gbp/grid_map-release.git
  tag: release/jazzy/grid_map_demos/2.2.2-2
  target_directory: ros2-grid-map-demos/src/work

build:
  script: ${{ '$RECIPE_DIR/build_ament_cmake.sh' if unix or wasm32 else '%RECIPE_DIR%\\bld_ament_cmake.bat' }}
  number: 21
  post_process:
    - files:
        - '*.pc'
      regex: (?:-L|-I)?"?([^;\s]+/sysroot/)
      replacement: $$(CONDA_BUILD_SYSROOT_S)
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/sysroot)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/MacOSX\d*\.?\d*\.sdk)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
about:
  homepage: http://github.com/anybotics/grid_map
  license: BSD-3-Clause
  summary: Demo nodes to demonstrate the usage of the grid map library.
extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - ${{ compiler('cxx') }}
    - ${{ compiler('c') }}
    - if: target_platform!='emscripten-wasm32'
      then:
        - ${{ stdlib('c') }}
    - ninja
    - python
    - setuptools
    - git
    - git-lfs
    - if: unix
      then:
        - patch
        - make
        - coreutils
    - if: win
      then:
        - m2-patch
    - if: osx
      then:
        - tapi
    - if: build_platform != target_platform
      then:
        - pkg-config
    - cmake
    - cython
    - if: build_platform != target_platform
      then:
        - python
        - cross-python_${{ target_platform }}
        - numpy
    - if: osx
      then:
        - clang-tools ${{ (cxx_compiler_version ~ '.*') if cxx_compiler_version is defined else '*' }}
  host:
    - if: build_platform == target_platform
      then:
        - pkg-config
    - numpy
    - pip
    - python
    - ros2-ament-cmake
    - ros2-ament-cmake-gtest
    - ros2-ament-lint-auto
    - ros2-ament-lint-common
    - ros2-cv-bridge
    - ros2-distro-mutex 0.16.* jazzy_*
    - ros2-geometry-msgs
    - ros2-grid-map-cmake-helpers
    - ros2-grid-map-core
    - ros2-grid-map-cv
    - ros2-grid-map-filters
    - ros2-grid-map-loader
    - ros2-grid-map-msgs
    - ros2-grid-map-octomap
    - ros2-grid-map-ros
    - ros2-grid-map-rviz-plugin
    - ros2-grid-map-visualization
    - ros2-octomap-msgs
    - ros2-rclcpp
    - ros2-ros-environment
    - ros2-ros-workspace
    - ros2-sensor-msgs
  run:
    - libopencv
    - py-opencv
    - python
    - ros2-cv-bridge
    - ros2-distro-mutex 0.16.* jazzy_*
    - ros2-geometry-msgs
    - ros2-grid-map-core
    - ros2-grid-map-cv
    - ros2-grid-map-filters
    - ros2-grid-map-loader
    - ros2-grid-map-msgs
    - ros2-grid-map-octomap
    - ros2-grid-map-ros
    - ros2-grid-map-rviz-plugin
    - ros2-grid-map-visualization
    - ros2-octomap-msgs
    - ros2-octomap-rviz-plugins
    - ros2-octomap-server
    - ros2-rclcpp
    - ros2-rclpy
    - ros2-ros-workspace
    - ros2-rviz2
    - ros2-sensor-msgs
