# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json

package:
  name: ros2-moveit2-tutorials
  version: 0.1.0
source:
  git: https://github.com/moveit/moveit2_tutorials.git
  rev: 27b5ed0232cd08de9631d26e823dccc6e8766513
  target_directory: ros2-moveit2-tutorials/src/work

build:
  script: ${{ '$RECIPE_DIR/build_ament_cmake.sh' if unix or wasm32 else '%RECIPE_DIR%\\bld_ament_cmake.bat' }}
  number: 21
  post_process:
    - files:
        - '*.pc'
      regex: (?:-L|-I)?"?([^;\s]+/sysroot/)
      replacement: $$(CONDA_BUILD_SYSROOT_S)
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/sysroot)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/MacOSX\d*\.?\d*\.sdk)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
about:
  homepage: https://moveit.picknik.ai
  repository: https://github.com/moveit/moveit2_tutorials
  license: BSD-3-Clause
  summary: The moveit2_tutorials package
extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - ${{ compiler('cxx') }}
    - ${{ compiler('c') }}
    - if: target_platform!='emscripten-wasm32'
      then:
        - ${{ stdlib('c') }}
    - ninja
    - python
    - setuptools
    - git
    - git-lfs
    - if: unix
      then:
        - patch
        - make
        - coreutils
    - if: win
      then:
        - m2-patch
    - if: osx
      then:
        - tapi
    - if: build_platform != target_platform
      then:
        - pkg-config
    - cmake
    - cython
    - if: build_platform != target_platform
      then:
        - python
        - cross-python_${{ target_platform }}
        - numpy
    - if: osx
      then:
        - clang-tools ${{ (cxx_compiler_version ~ '.*') if cxx_compiler_version is defined else '*' }}
  host:
    - ${{ 'libgl-devel' if linux }}
    - ${{ 'libopengl-devel' if linux }}
    - if: build_platform == target_platform
      then:
        - pkg-config
    - eigen
    - eigen-abi-devel
    - numpy
    - pip
    - python
    - ros2-ament-cmake
    - ros2-ament-cmake-gtest
    - ros2-control-msgs
    - ros2-distro-mutex 0.16.* jazzy_*
    - ros2-geometric-shapes
    - ros2-interactive-markers
    - ros2-moveit-common
    - ros2-moveit-core
    - ros2-moveit-hybrid-planning
    - ros2-moveit-msgs
    - ros2-moveit-ros-perception
    - ros2-moveit-ros-planning
    - ros2-moveit-ros-planning-interface
    - ros2-moveit-ros-trajectory-cache
    - ros2-moveit-servo
    - ros2-moveit-task-constructor-core
    - ros2-moveit-visual-tools
    - ros2-pluginlib
    - ros2-ros-environment
    - ros2-ros-testing
    - ros2-ros-workspace
    - ros2-rviz-visual-tools
    - ros2-tf2-eigen
    - ros2-tf2-geometry-msgs
    - ros2-tf2-ros
    - ros2-warehouse-ros-sqlite
  run:
    - notebook
    - python
    - ros2-control-msgs
    - ros2-controller-manager
    - ros2-distro-mutex 0.16.* jazzy_*
    - ros2-interactive-markers
    - ros2-joint-state-broadcaster
    - ros2-joint-state-publisher
    - ros2-joint-trajectory-controller
    - ros2-joy
    - ros2-kinova-gen3-7dof-robotiq-2f-85-moveit-config
    - ros2-moveit
    - ros2-moveit-configs-utils
    - ros2-moveit-core
    - ros2-moveit-hybrid-planning
    - ros2-moveit-msgs
    - ros2-moveit-py
    - ros2-moveit-resources-panda-moveit-config
    - ros2-moveit-ros-perception
    - ros2-moveit-ros-planning-interface
    - ros2-moveit-ros-trajectory-cache
    - ros2-moveit-servo
    - ros2-moveit-task-constructor-core
    - ros2-moveit-visual-tools
    - ros2-pluginlib
    - ros2-robot-state-publisher
    - ros2-ros-workspace
    - ros2-ros2-control
    - ros2-rviz-visual-tools
    - ros2-rviz2
    - ros2-tf2-eigen
    - ros2-tf2-geometry-msgs
    - ros2-tf2-ros
    - ros2-warehouse-ros-sqlite
    - ros2-xacro
