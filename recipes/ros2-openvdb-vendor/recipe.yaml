# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json

package:
  name: ros2-openvdb-vendor
  version: 2.5.5
source:
  git: https://github.com/SteveMacenski/spatio_temporal_voxel_layer-release.git
  tag: release/jazzy/openvdb_vendor/2.5.5-1
  target_directory: ros2-openvdb-vendor/src/work
  patches:
    - patch/ros-jazzy-openvdb-vendor.patch

build:
  script: ${{ '$RECIPE_DIR/build_ament_cmake.sh' if unix or wasm32 else '%RECIPE_DIR%\\bld_ament_cmake.bat' }}
  number: 21
  post_process:
    - files:
        - '*.pc'
      regex: (?:-L|-I)?"?([^;\s]+/sysroot/)
      replacement: $$(CONDA_BUILD_SYSROOT_S)
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/sysroot)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/MacOSX\d*\.?\d*\.sdk)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
about:
  homepage: https://github.com/AcademySoftwareFoundation/openvdb
  license: LGPL-2.1-or-later OR MPL-2.0
  summary: Wrapper around OpenVDB, if not found on the system, will compile from source
extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - ${{ compiler('cxx') }}
    - ${{ compiler('c') }}
    - if: target_platform!='emscripten-wasm32'
      then:
        - ${{ stdlib('c') }}
    - ninja
    - python
    - setuptools
    - git
    - git-lfs
    - if: unix
      then:
        - patch
        - make
        - coreutils
    - if: win
      then:
        - m2-patch
    - if: osx
      then:
        - tapi
    - if: build_platform != target_platform
      then:
        - pkg-config
    - cmake
    - cython
    - if: build_platform != target_platform
      then:
        - python
        - cross-python_${{ target_platform }}
        - numpy
    - if: osx
      then:
        - clang-tools ${{ (cxx_compiler_version ~ '.*') if cxx_compiler_version is defined else '*' }}
  host:
    - blosc
    - if: build_platform == target_platform
      then:
        - pkg-config
    - libboost-devel
    - numpy
    - openvdb
    - pip
    - python
    - ros2-ament-cmake
    - ros2-ament-cmake-vendor-package
    - ros2-distro-mutex 0.16.* jazzy_*
    - ros2-ros-environment
    - ros2-ros-workspace
    - tbb
    - tbb-devel
    - zlib
  run:
    - blosc
    - libboost-devel
    - openvdb
    - python
    - ros2-distro-mutex 0.16.* jazzy_*
    - ros2-ros-workspace
    - tbb
    - tbb-devel
    - zlib
