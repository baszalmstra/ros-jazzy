# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json

package:
  name: ros2-nav2-costmap-2d
  version: 1.3.12
source:
  git: https://github.com/SteveMacenski/navigation2-release.git
  tag: release/jazzy/nav2_costmap_2d/1.3.12-1
  target_directory: ros2-nav2-costmap-2d/src/work
  patches:
    - patch/ros-jazzy-nav2-costmap-2d.patch

build:
  script: ${{ '$RECIPE_DIR/build_ament_cmake.sh' if unix or wasm32 else '%RECIPE_DIR%\\bld_ament_cmake.bat' }}
  number: 21
  post_process:
    - files:
        - '*.pc'
      regex: (?:-L|-I)?"?([^;\s]+/sysroot/)
      replacement: $$(CONDA_BUILD_SYSROOT_S)
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/sysroot)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/MacOSX\d*\.?\d*\.sdk)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
about:
  license: BSD-3-Clause OR Apache-2.0
  summary: "This package provides an implementation of a 2D costmap that takes in sensor\n    data from the world, builds a 2D or 3D occupancy grid of the data (depending\n    on whether a voxel based implementation is used), and inflates costs in a\n    2D costmap based on the occupancy grid and a user specified inflation radius.\n    This package also provides support for map_server based initialization of a\n    costmap, rolling window based costmaps, and parameter based subscription to\n    and configuration of sensor topics."
extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - ${{ compiler('cxx') }}
    - ${{ compiler('c') }}
    - if: target_platform!='emscripten-wasm32'
      then:
        - ${{ stdlib('c') }}
    - ninja
    - python
    - setuptools
    - git
    - git-lfs
    - if: unix
      then:
        - patch
        - make
        - coreutils
    - if: win
      then:
        - m2-patch
    - if: osx
      then:
        - tapi
    - if: build_platform != target_platform
      then:
        - pkg-config
    - cmake
    - cython
    - if: build_platform != target_platform
      then:
        - python
        - cross-python_${{ target_platform }}
        - numpy
    - if: osx
      then:
        - clang-tools ${{ (cxx_compiler_version ~ '.*') if cxx_compiler_version is defined else '*' }}
  host:
    - if: build_platform == target_platform
      then:
        - pkg-config
    - numpy
    - pip
    - python
    - ros2-ament-cmake
    - ros2-ament-cmake-gtest
    - ros2-ament-lint-auto
    - ros2-ament-lint-common
    - ros2-angles
    - ros2-distro-mutex 0.16.* jazzy_*
    - ros2-geometry-msgs
    - ros2-laser-geometry
    - ros2-launch
    - ros2-launch-testing
    - ros2-map-msgs
    - ros2-message-filters
    - ros2-nav-msgs
    - ros2-nav2-common
    - ros2-nav2-lifecycle-manager
    - ros2-nav2-map-server
    - ros2-nav2-msgs
    - ros2-nav2-util
    - ros2-nav2-voxel-grid
    - ros2-pluginlib
    - ros2-rclcpp
    - ros2-rclcpp-lifecycle
    - ros2-ros-environment
    - ros2-ros-workspace
    - ros2-sensor-msgs
    - ros2-std-msgs
    - ros2-std-srvs
    - ros2-tf2
    - ros2-tf2-geometry-msgs
    - ros2-tf2-ros
    - ros2-tf2-sensor-msgs
    - ros2-visualization-msgs
  run:
    - python
    - ros2-angles
    - ros2-distro-mutex 0.16.* jazzy_*
    - ros2-geometry-msgs
    - ros2-laser-geometry
    - ros2-map-msgs
    - ros2-message-filters
    - ros2-nav-msgs
    - ros2-nav2-msgs
    - ros2-nav2-util
    - ros2-nav2-voxel-grid
    - ros2-pluginlib
    - ros2-rclcpp
    - ros2-rclcpp-lifecycle
    - ros2-ros-workspace
    - ros2-sensor-msgs
    - ros2-std-msgs
    - ros2-std-srvs
    - ros2-tf2
    - ros2-tf2-geometry-msgs
    - ros2-tf2-ros
    - ros2-tf2-sensor-msgs
    - ros2-visualization-msgs
