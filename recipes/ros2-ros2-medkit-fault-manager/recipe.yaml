# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json

package:
  name: ros2-ros2-medkit-fault-manager
  version: 0.6.0
source:
  git: https://github.com/ros2-gbp/ros2_medkit-release.git
  tag: release/jazzy/ros2_medkit_fault_manager/0.6.0-1
  target_directory: ros2-ros2-medkit-fault-manager/src/work

build:
  script: ${{ '$RECIPE_DIR/build_ament_cmake.sh' if unix or wasm32 else '%RECIPE_DIR%\\bld_ament_cmake.bat' }}
  number: 21
  post_process:
    - files:
        - '*.pc'
      regex: (?:-L|-I)?"?([^;\s]+/sysroot/)
      replacement: $$(CONDA_BUILD_SYSROOT_S)
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/sysroot)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/MacOSX\d*\.?\d*\.sdk)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
about:
  license: Apache-2.0
  summary: Central fault manager node for ros2_medkit fault management system
extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - ${{ compiler('cxx') }}
    - ${{ compiler('c') }}
    - if: target_platform!='emscripten-wasm32'
      then:
        - ${{ stdlib('c') }}
    - ninja
    - python
    - setuptools
    - git
    - git-lfs
    - if: unix
      then:
        - patch
        - make
        - coreutils
    - if: win
      then:
        - m2-patch
    - if: osx
      then:
        - tapi
    - if: build_platform != target_platform
      then:
        - pkg-config
    - cmake
    - cython
    - if: build_platform != target_platform
      then:
        - python
        - cross-python_${{ target_platform }}
        - numpy
    - if: osx
      then:
        - clang-tools ${{ (cxx_compiler_version ~ '.*') if cxx_compiler_version is defined else '*' }}
  host:
    - if: build_platform == target_platform
      then:
        - pkg-config
    - nlohmann_json
    - numpy
    - pip
    - python
    - ros2-ament-cmake
    - ros2-ament-cmake-clang-format
    - ros2-ament-cmake-clang-tidy
    - ros2-ament-cmake-gtest
    - ros2-ament-lint-auto
    - ros2-ament-lint-common
    - ros2-distro-mutex 0.16.* jazzy_*
    - ros2-launch-ros
    - ros2-launch-testing-ament-cmake
    - ros2-launch-testing-ros
    - ros2-rclcpp
    - ros2-ros-environment
    - ros2-ros-workspace
    - ros2-ros2-medkit-cmake
    - ros2-ros2-medkit-msgs
    - ros2-ros2-medkit-serialization
    - ros2-rosbag2-cpp
    - ros2-rosbag2-py
    - ros2-rosbag2-storage
    - ros2-rosbag2-storage-mcap
    - ros2-sensor-msgs
    - ros2-std-msgs
    - sqlite 3.*
  run:
    - nlohmann_json
    - python
    - ros2-ament-index-python
    - ros2-distro-mutex 0.16.* jazzy_*
    - ros2-launch
    - ros2-launch-ros
    - ros2-rclcpp
    - ros2-ros-workspace
    - ros2-ros2-medkit-msgs
    - ros2-ros2-medkit-serialization
    - ros2-rosbag2-cpp
    - ros2-rosbag2-storage
    - sqlite
