# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json

package:
  name: ros2-gz-fuel-tools-vendor
  version: 0.0.6
source:
  git: https://github.com/ros2-gbp/gz_fuel_tools_vendor-release.git
  tag: release/jazzy/gz_fuel_tools_vendor/0.0.6-1
  target_directory: ros2-gz-fuel-tools-vendor/src/work
  patches:
    - patch/ros-jazzy-gz-fuel-tools-vendor.patch

build:
  script: ${{ '$RECIPE_DIR/build_ament_cmake.sh' if unix or wasm32 else '%RECIPE_DIR%\\bld_ament_cmake.bat' }}
  number: 21
  post_process:
    - files:
        - '*.pc'
      regex: (?:-L|-I)?"?([^;\s]+/sysroot/)
      replacement: $$(CONDA_BUILD_SYSROOT_S)
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/sysroot)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/MacOSX\d*\.?\d*\.sdk)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
about:
  homepage: https://github.com/gazebosim/gz-fuel-tools
  license: Apache-2.0
  summary: "Vendor package for: gz-fuel_tools9 9.1.1\n\n    Gazebo Fuel Tools: Classes and tools for interacting with Gazebo Fuel"
extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - ${{ compiler('cxx') }}
    - ${{ compiler('c') }}
    - if: target_platform!='emscripten-wasm32'
      then:
        - ${{ stdlib('c') }}
    - ninja
    - python
    - setuptools
    - git
    - git-lfs
    - if: unix
      then:
        - patch
        - make
        - coreutils
    - if: win
      then:
        - m2-patch
    - if: osx
      then:
        - tapi
    - if: build_platform != target_platform
      then:
        - pkg-config
    - cmake
    - cython
    - if: build_platform != target_platform
      then:
        - python
        - cross-python_${{ target_platform }}
        - numpy
    - if: osx
      then:
        - clang-tools ${{ (cxx_compiler_version ~ '.*') if cxx_compiler_version is defined else '*' }}
  host:
    - if: build_platform == target_platform
      then:
        - pkg-config
    - gflags
    - gz-fuel-tools9
    - jsoncpp
    - libcurl
    - libzip
    - numpy
    - pip
    - python
    - ros2-ament-cmake-copyright
    - ros2-ament-cmake-core
    - ros2-ament-cmake-lint-cmake
    - ros2-ament-cmake-test
    - ros2-ament-cmake-vendor-package
    - ros2-ament-cmake-xmllint
    - ros2-distro-mutex 0.16.* jazzy_*
    - ros2-gz-cmake-vendor
    - ros2-gz-common-vendor
    - ros2-gz-math-vendor
    - ros2-gz-msgs-vendor
    - ros2-gz-tools-vendor
    - ros2-gz-utils-vendor
    - ros2-ros-environment
    - ros2-ros-workspace
    - tinyxml2
    - yaml
  run:
    - gflags
    - gz-fuel-tools9
    - jsoncpp
    - libcurl
    - libzip
    - python
    - ros2-distro-mutex 0.16.* jazzy_*
    - ros2-gz-cmake-vendor
    - ros2-gz-common-vendor
    - ros2-gz-math-vendor
    - ros2-gz-msgs-vendor
    - ros2-gz-tools-vendor
    - ros2-gz-utils-vendor
    - ros2-ros-workspace
    - tinyxml2
    - yaml
