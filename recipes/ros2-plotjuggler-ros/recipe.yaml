# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json

package:
  name: ros2-plotjuggler-ros
  version: 2.3.1
source:
  git: https://github.com/ros2-gbp/plotjuggler-ros-plugins-release.git
  tag: release/jazzy/plotjuggler_ros/2.3.1-1
  target_directory: ros2-plotjuggler-ros/src/work

build:
  script: ${{ '$RECIPE_DIR/build_ament_cmake.sh' if unix or wasm32 else '%RECIPE_DIR%\\bld_ament_cmake.bat' }}
  number: 21
  post_process:
    - files:
        - '*.pc'
      regex: (?:-L|-I)?"?([^;\s]+/sysroot/)
      replacement: $$(CONDA_BUILD_SYSROOT_S)
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/sysroot)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/MacOSX\d*\.?\d*\.sdk)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
about:
  homepage: https://github.com/facontidavide/PlotJuggler
  license: AGPL-3.0-only
  summary: PlotJuggler plugin for ROS
extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - ${{ compiler('cxx') }}
    - ${{ compiler('c') }}
    - if: target_platform!='emscripten-wasm32'
      then:
        - ${{ stdlib('c') }}
    - ninja
    - python
    - setuptools
    - git
    - git-lfs
    - if: unix
      then:
        - patch
        - make
        - coreutils
    - if: win
      then:
        - m2-patch
    - if: osx
      then:
        - tapi
    - if: build_platform != target_platform
      then:
        - pkg-config
    - cmake
    - cython
    - if: build_platform != target_platform
      then:
        - python
        - cross-python_${{ target_platform }}
        - numpy
    - if: osx
      then:
        - clang-tools ${{ (cxx_compiler_version ~ '.*') if cxx_compiler_version is defined else '*' }}
    - if: build_platform != target_platform
      then:
        - qt-main
  host:
    - ${{ 'libgl-devel' if linux }}
    - ${{ 'libopengl-devel' if linux }}
    - binutils
    - if: build_platform == target_platform
      then:
        - pkg-config
    - fmt
    - libboost-devel
    - libboost-python-devel
    - libgl-devel
    - libopengl-devel
    - numpy
    - pip
    - python
    - qt-main
    - ros2-ament-cmake
    - ros2-distro-mutex 0.16.* jazzy_*
    - ros2-plotjuggler
    - ros2-plotjuggler-msgs
    - ros2-rclcpp
    - ros2-rcpputils
    - ros2-ros-environment
    - ros2-ros-workspace
    - ros2-rosbag2-transport
    - ros2-tf2-msgs
    - ros2-tf2-ros
  run:
    - binutils
    - fmt
    - libboost-devel
    - libboost-python-devel
    - libgl-devel
    - libopengl-devel
    - python
    - qt-main
    - ros2-distro-mutex 0.16.* jazzy_*
    - ros2-plotjuggler
    - ros2-plotjuggler-msgs
    - ros2-rclcpp
    - ros2-rcpputils
    - ros2-ros-workspace
    - ros2-rosbag2-transport
    - ros2-tf2-msgs
    - ros2-tf2-ros
