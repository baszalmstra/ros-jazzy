# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json

package:
  name: ros2-rosbag2-storage-sqlite3
  version: 0.26.11
source:
  git: https://github.com/ros2-gbp/rosbag2-release.git
  tag: release/jazzy/rosbag2_storage_sqlite3/0.26.11-1
  target_directory: ros2-rosbag2-storage-sqlite3/src/work

build:
  script: ${{ '$RECIPE_DIR/build_ament_cmake.sh' if unix or wasm32 else '%RECIPE_DIR%\\bld_ament_cmake.bat' }}
  number: 21
  post_process:
    - files:
        - '*.pc'
      regex: (?:-L|-I)?"?([^;\s]+/sysroot/)
      replacement: $$(CONDA_BUILD_SYSROOT_S)
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/sysroot)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/MacOSX\d*\.?\d*\.sdk)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
about:
  license: Apache-2.0
  summary: ROSBag2 SQLite3 storage plugin
extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - ${{ compiler('cxx') }}
    - ${{ compiler('c') }}
    - if: target_platform!='emscripten-wasm32'
      then:
        - ${{ stdlib('c') }}
    - ninja
    - python
    - setuptools
    - git
    - git-lfs
    - if: unix
      then:
        - patch
        - make
        - coreutils
    - if: win
      then:
        - m2-patch
    - if: osx
      then:
        - tapi
    - if: build_platform != target_platform
      then:
        - pkg-config
    - cmake
    - cython
    - if: build_platform != target_platform
      then:
        - python
        - cross-python_${{ target_platform }}
        - numpy
    - if: osx
      then:
        - clang-tools ${{ (cxx_compiler_version ~ '.*') if cxx_compiler_version is defined else '*' }}
  host:
    - if: build_platform == target_platform
      then:
        - pkg-config
    - numpy
    - pip
    - python
    - ros2-ament-cmake
    - ros2-ament-cmake-gmock
    - ros2-ament-cmake-python
    - ros2-ament-lint-auto
    - ros2-ament-lint-common
    - ros2-distro-mutex 0.16.* jazzy_*
    - ros2-pluginlib
    - ros2-rcpputils
    - ros2-rcutils
    - ros2-ros-environment
    - ros2-ros-workspace
    - ros2-rosbag2-storage
    - ros2-rosbag2-test-common
    - ros2-sqlite3-vendor
    - ros2-std-msgs
    - ros2-yaml-cpp-vendor
  run:
    - python
    - ros2-distro-mutex 0.16.* jazzy_*
    - ros2-pluginlib
    - ros2-rcpputils
    - ros2-rcutils
    - ros2-ros-workspace
    - ros2-rosbag2-storage
    - ros2-sqlite3-vendor
    - ros2-yaml-cpp-vendor
