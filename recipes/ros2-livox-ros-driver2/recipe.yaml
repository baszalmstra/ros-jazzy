# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json

package:
  name: ros2-livox-ros-driver2
  version: 1.2.4
source:
  git: https://github.com/Livox-SDK/livox_ros_driver2.git
  tag: 1.2.4
  target_directory: ros2-livox-ros-driver2/src/work
  patches:
    - patch/ros-jazzy-livox-ros-driver2.patch

build:
  script: ${{ '$RECIPE_DIR/build_ament_cmake.sh' if unix or wasm32 else '%RECIPE_DIR%\\bld_ament_cmake.bat' }}
  number: 21
  post_process:
    - files:
        - '*.pc'
      regex: (?:-L|-I)?"?([^;\s]+/sysroot/)
      replacement: $$(CONDA_BUILD_SYSROOT_S)
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/sysroot)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/MacOSX\d*\.?\d*\.sdk)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
about:
  license: MIT
  summary: The ROS device driver for Livox 3D LiDARs, for ROS2
extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - ${{ compiler('cxx') }}
    - ${{ compiler('c') }}
    - if: target_platform!='emscripten-wasm32'
      then:
        - ${{ stdlib('c') }}
    - ninja
    - python
    - setuptools
    - git
    - git-lfs
    - if: unix
      then:
        - patch
        - make
        - coreutils
    - if: win
      then:
        - m2-patch
    - if: osx
      then:
        - tapi
    - if: build_platform != target_platform
      then:
        - pkg-config
    - cmake
    - cython
    - if: build_platform != target_platform
      then:
        - python
        - cross-python_${{ target_platform }}
        - numpy
    - if: osx
      then:
        - clang-tools ${{ (cxx_compiler_version ~ '.*') if cxx_compiler_version is defined else '*' }}
    - if: target_platform == 'emscripten-wasm32'
      then:
        - ros2-rosidl-default-generators
    - if: build_platform != target_platform
      then:
        - git
  host:
    - apr
    - if: build_platform == target_platform
      then:
        - pkg-config
    - libboost-devel
    - libgl-devel
    - libopengl-devel
    - livox-sdk2
    - numpy
    - pcl
    - pip
    - python
    - ros2-ament-cmake-auto
    - ros2-ament-lint-auto
    - ros2-ament-lint-common
    - ros2-distro-mutex 0.16.* jazzy_*
    - ros2-pcl-conversions
    - ros2-rcl-interfaces
    - ros2-rclcpp
    - ros2-rclcpp-components
    - ros2-rcutils
    - ros2-ros-environment
    - ros2-ros-workspace
    - ros2-rosidl-default-generators
    - ros2-sensor-msgs
    - ros2-std-msgs
    - vtk-base
    - if: build_platform == target_platform
      then:
        - git
  run:
    - apr
    - git
    - libboost-devel
    - libgl-devel
    - libopengl-devel
    - pcl
    - python
    - ros2-distro-mutex 0.16.* jazzy_*
    - ros2-pcl-conversions
    - ros2-rcl-interfaces
    - ros2-rclcpp
    - ros2-rclcpp-components
    - ros2-rcutils
    - ros2-ros-workspace
    - ros2-rosbag2
    - ros2-rosidl-default-runtime
    - ros2-sensor-msgs
    - ros2-std-msgs
    - vtk-base
